#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: the value to be converted into absolute
 *
 * Return: absolute value of i
 */
int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	return (i);
}
